# Security and voice-use policy

## 只处理有权使用的声音

本项目的声音克隆能力只适用于声音所有者本人，或已经取得单独、明确、可追踪授权的声音。不要用它冒充他人、绕过身份核验、制作欺骗内容，或超出授权目的和期限复用声音。

`authorized: true` 是本地工作流中的声明与门禁输入，不是身份真实性证明。实际使用者仍需负责核验授权人与授权范围。

## 默认本地、默认私有、默认不发布

- 参考录音、准确逐字稿、授权证据、生成 WAV、模型和运行记录应留在生产包的 `private/` 目录；
- 仓库的 `.gitignore` 默认排除常见音频、模型和私有目录，但它不能代替提交前人工检查；
- 未经声音所有者另行授权，不要把参考或生成音频上传到第三方服务；
- 机器 QA、本人选择、视频人工审核和平台发布是不同状态，不能互相代替。

## 报告问题

如果你发现会泄露声音资产、绕过授权/人工选择、伪造发布状态或执行路径穿越的问题，请不要在公开 Issue 中附带任何真实录音、授权文件、模型或平台凭据。请先通过仓库维护者提供的私密安全联系方式报告；若仓库尚未发布，请直接联系交付此副本的人。

报告应包含最小复现步骤、受影响版本和不含个人数据的合成样例。不要上传第三方声音来证明漏洞。
